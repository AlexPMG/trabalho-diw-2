{
  "name": "pokewiki-json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "server": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.0",
    "cors": "^2.8.5"
  }
}